{
  "name": "@shake2/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cdk": "cdk",
    "synth": "npm run build && cdk synth --app \"node dist/app.js\"",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "aws-cdk": "^2.1125.0",
    "aws-cdk-lib": "^2.257.0",
    "constructs": "^10.6.0",
    "esbuild": "^0.28.0"
  }
}
